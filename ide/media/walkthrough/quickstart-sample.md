# Open a sample file

Open the bundled quick sample so the language server can attach to a real `.ncg` document.

Use the button in the step description, or run **NiceGeo: Open walkthrough quick sample** from the Command Palette.
