# Practice in the tutorial file

Work **top to bottom**:

1. **Tactic blocks** — step the caret through `walkthrough_id_tactic`, `walkthrough_and_elim`, and `walkthrough_and_swap`; match what you see in **Proof State** to the comments at the top of the file.
2. **Hover / F12** — in the later theorems, hover `walkthrough_dup`, `TrueI`, and introduced names; use **go to definition** on globals.
3. **Contrast** — at the bottom, `walkthrough_demo_term_only` uses `:=` without `Proof.`; Proof Mode is aimed at the script style above it.

When you are finished, use **NiceGeo: Exit Proof Mode** if you want to return to a quieter editor, or leave it on for your own `.ncg` files.
